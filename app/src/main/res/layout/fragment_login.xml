<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.mirsoft.easyfixmaster.LoginFragment"
    android:background="@color/white_blue"
    android:weightSum="1"
    android:id="@+id/zz"
    android:paddingLeft="20dp">

    <!-- TODO: Update blank fragment layout -->


    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/scrollView" >

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center_vertical">

            <com.rengwuxian.materialedittext.MaterialEditText
                style="@style/EditText"
                app:met_baseColor="@color/blacktext"
                app:met_primaryColor="@color/input_base_blue"
                app:met_underlineColor="@color/gray"
                app:met_errorColor="@color/red"
                app:met_helperText="@string/first_name"
                android:hint="@string/first_name"
                android:inputType="textPersonName"
                android:id="@+id/etLogin"
                android:layout_centerVertical="true"
                android:layout_alignParentTop="true"
                android:layout_centerHorizontal="true"/>

            <com.rengwuxian.materialedittext.MaterialEditText
                style="@style/EditText"
                app:met_baseColor="@color/blacktext"
                app:met_primaryColor="@color/input_base_blue"
                app:met_underlineColor="@color/gray"
                app:met_errorColor="@color/red"
                app:met_helperText="@string/password"
                android:hint="@string/password"
                android:inputType="textPassword"
                android:id="@+id/etPassword"
                android:layout_below="@+id/etLogin" />

            <com.dd.processbutton.FlatButton
                android:layout_width="250dp"
                android:layout_height="wrap_content"
                android:text="@string/login"
                android:id="@+id/btnSubmit"
                android:layout_below="@id/etPassword"
                android:layout_centerHorizontal="true"
                app:pb_colorNormal="@android:color/holo_blue_light"
                app:pb_colorPressed="@android:color/holo_blue_dark"
                app:pb_colorProgress="@color/purple_progress"
                android:paddingLeft="20dp"
                android:paddingRight="20dp"
                android:layout_marginTop="30dp" />

            <com.dd.processbutton.FlatButton
                android:layout_width="250dp"
                android:layout_height="wrap_content"
                android:text="@string/forget_password"
                android:id="@+id/forgetPassword"
                android:layout_below="@id/btnSubmit"
                android:layout_centerHorizontal="true"
                android:background="@android:color/transparent"
                android:paddingLeft="20dp"
                android:paddingRight="20dp"
                android:layout_marginTop="30dp" />
        </RelativeLayout>
    </ScrollView>

</RelativeLayout>
